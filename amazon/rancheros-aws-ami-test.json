{
    "variables": {
        "version": "{{env `RANCHEROS_VERSION`}}"
    },
    "builders": [
        {
            "type": "amazon-ebs",
            "region": "us-west-1",
            "ami_regions": [
                "us-west-1"
            ],
            "source_ami": "ami-0757b143",
            "instance_type": "m3.medium",
            "ssh_username": "rancher",
            "ami_name": "rancheros-{{user `version`}}-test-{{timestamp}}"
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "script": "../scripts/install2disk"
        }
    ]
}
