cat > /tmp/network_config.yml<<EOF
network:
  interfaces:
    - dhcp: true
      match: eth0
    - address: 127.0.0.1/8
      match: lo
EOF

rancherctl config merge < /tmp/network_config.yml
rm /tmp/network_config.yml
